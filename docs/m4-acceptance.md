# M4 验收标准（分层）

> 生效时间：2026-08-28。M4 范围：提示词块库（服务端持久化 + 设置页管理）、运行详情完善（重跑失败节点、日志弹窗、复制/下载）、运行记录页筛选、数据与工程设置页。
> 验收原则：L0/L1/L2 全自动；L3 用 bsk 真实浏览器；M4 不依赖外部付费能力。

## 2026-08-28 验收结果

| 层级 | 结果 | 证据 |
|---|---|---|
| L0 静态质量 | ✅ 通过 | typecheck / test（9 用例）/ build / lint 全绿 |
| L1 API 自检 | ✅ 12/12 | `pnpm check:api:m4`（提示词块 CRUD、运行日志、按节点过滤、数据信息） |
| L2 无头 CDP | ✅ 34/34 | `pnpm smoke:ui`（含运行详情日志弹窗、提示词块库、数据与工程页） |
| L3 真实浏览器 | ✅ 通过 | bsk 会话：提示词块库渲染内置 3 块，UI 新增自定义块「会议纪要提炼」成功并展示 |
| L4 外部能力 | 不适用 | M4 无外部付费依赖 |

## L0 静态质量

- `pnpm typecheck` 0 错误
- `pnpm test`：shared 4 + bili 2 + ai 3
- `pnpm build` 成功
- `pnpm lint`：slop / ui-lint 全过

## L1 API 自检（`pnpm check:api:m4`，12 项）

| # | 检查 |
|---|---|
| 1 | GET /api/prompts 内置 3 块 |
| 2 | POST 创建自定义块 |
| 3 | PATCH 更新自定义块 |
| 4 | 自定义块进入列表 |
| 5 | 文本链路运行成功（用于日志） |
| 6 | GET /api/runs/:id/logs 有日志 |
| 7 | 日志包含 input 与输出文件 info |
| 8 | 日志可按节点过滤 |
| 9 | GET /api/settings/data 返回计数 |
| 10 | DELETE 自定义块 |
| 11 | 内置块不可删除（400） |
| 12 | 清理临时数据 |

## L2 无头 CDP（`pnpm smoke:ui`，34 项）

M3 30 项基础上新增 4 项：

| # | 检查 |
|---|---|
| 31 | 运行详情渲染输出文档 |
| 32 | 日志弹窗展示节点日志 |
| 33 | 提示词块库渲染内置 3 块 |
| 34 | 数据与工程页渲染数据信息 |

## L3 真实浏览器（bsk）

| # | 检查 | 结果 |
|---|---|---|
| 1 | /settings → 提示词块库：内置 3 块只读展示、预览摘要 | ✅ |
| 2 | 新增自定义块：填写名称/提示词 → 保存 → 列表出现「会议纪要提炼」 | ✅ |
| 3 | 数据与工程页数据信息渲染（headless） | ✅ |
| 4 | 运行详情日志弹窗（headless + API 双路径） | ✅ |

## 完成判据

L0–L3 全部通过，M4 完成 ✅（2026-08-28）。
